# 🐱 Мяу Разделитель Файлов

Простой и удобный Python инструмент для разделения больших файлов на части и их последующего объединения. С котиками! 😺

## ✨ Особенности

* 📦 Разделение файлов на N равных частей
* 📏 Разделение файлов по заданному размеру части
* 🔗 Автоматическое объединение частей обратно в файл
* 🔐 Проверка целостности с помощью SHA-256
* 📁 Автоматическое создание папок для частей
* 📊 Информационные файлы для легкого восстановления
* 🎨 Красочный интерфейс с ASCII котиками
* 🔊 Звуковые уведомления
* 💾 Поддержка больших файлов (до нескольких терабайт)

## 🚀 Установка

1. Убедитесь, что у вас установлен Python 3.6 или выше
2. Скачайте файл `main.py`
3. Готово! Никаких дополнительных зависимостей не требуется

```bash
git clone https://github.com/yourusername/file-splitter.git
cd file-splitter
```

## 💻 Использование

### Интерактивный режим

Запустите программу без аргументов для входа в интерактивное меню:

```bash
python main.py
```

### Режим командной строки

#### Разделить файл на N частей

```bash
python main.py --split имя_файла.ext 5
```

Разделит файл на 5 равных частей.

#### Разделить файл по размеру части

```bash
python main.py --split-size имя_файла.ext 10MB
```

Поддерживаемые единицы измерения: B, KB, MB, GB

Примеры:
* `100KB` = 102,400 байт
* `10MB` = 10,485,760 байт
* `1GB` = 1,073,741,824 байта

#### Объединить части файла

```bash
python main.py --join папка_с_частями
```

Или с указанием имени результирующего файла:

```bash
python main.py --join папка_с_частями восстановленный_файл.ext
```

#### Справка

```bash
python main.py --help
```

## 📖 Примеры использования

### Разделение большого видео файла

```bash
python main.py --split-size video.mp4 500MB
```

Создаст папку `video_parts` с частями по 500 МБ каждая.

### Разделение файла для загрузки в облако

```bash
python main.py --split document.pdf 3
```

Разделит документ на 3 равные части.

### Восстановление файла

```bash
python main.py --join video_parts
```

Автоматически определит имя исходного файла из информационного файла.

## 🗂️ Структура выходных файлов

После разделения создается папка с такой структурой:

```
имя_файла_parts/
├── имя_файла.ext              # Исходный файл (перемещен сюда)
├── имя_файла_part_001.ext     # Часть 1
├── имя_файла_part_002.ext     # Часть 2
├── имя_файла_part_003.ext     # Часть 3
└── !split_info.txt            # Информация о разделении
```

## 🔒 Проверка целостности

Программа автоматически:
1. Вычисляет SHA-256 хеш исходного файла перед разделением
2. Сохраняет его в информационном файле
3. Проверяет хеш после объединения
4. Предлагает удалить папку с частями при успешной проверке

## 📋 Требования

* Python 3.6+
* Стандартная библиотека Python (без внешних зависимостей)

## 🛠️ Технические детали

* Размер чанка для чтения: 4096 байт
* Алгоритм хеширования: SHA-256
* Поддержка Unicode имен файлов
* Безопасное перемещение исходного файла
* Автоматический расчет размеров частей

## 🤝 Вклад в проект

Вклад в проект приветствуется! Не стесняйтесь создавать issues или pull requests.

## 📝 Лицензия

MIT License

## 🐾 Автор

Создано с любовью к котикам и программированию! 🐱💻

## ⚠️ Предупреждения

* Всегда делайте резервные копии важных файлов перед разделением
* Убедитесь, что на диске достаточно места для создания частей
* При объединении файлов проверяйте хеш для гарантии целостности

## 🎯 Планы на будущее

* [ ] Поддержка архивирования частей
* [ ] Шифрование частей файлов
* [ ] GUI интерфейс
* [ ] Поддержка параллельного разделения
* [ ] Интеграция с облачными хранилищами

## 📞 Поддержка

Если у вас возникли проблемы или вопросы, создайте issue в репозитории GitHub.

Мяу! 😺